function [out] = histEqualization(input_image)
    hist_temp = zeros(1,256,"single");
    [hist_temp] = imageHist(input_image);

    sum = 0;
    
    for i = 1:256
        sum = sum + hist_temp[i];
    end

    1/sum

    out = hist_vector_temp;
end

